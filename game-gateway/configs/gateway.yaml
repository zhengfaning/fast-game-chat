server:
  host: "0.0.0.0"
  port: 8080
  env: "dev" # set to 'prod' to disable pprof

redis:
  addr: "localhost:6379"
  password: ""

mq:
  type: "robustmq" # Using RobustMQ for better performance and reliability
  robustmq:
    broker: "tcp://localhost:1883"
    client_id: "gateway-1"
    username: ""
    password: ""
  redis:
    addr: "localhost:6379"
    password: ""

games:
  - id: "mmo"
    game_backend:
      host: "localhost"
      port: 9001
      pool_size: 100
    chat_backend:
      host: "localhost"
      port: 9002
      pool_size: 1  # 修改为 1，解决多连接消息丢失问题
